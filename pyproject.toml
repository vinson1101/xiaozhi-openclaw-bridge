[project]
name = "xiaozhi-openclaw-bridge"
version = "0.1.0"
description = "Private ESP32 voice terminal bridge for OpenClaw, Hermes, and Zebra-backed agents"
requires-python = ">=3.9"
dependencies = []

[project.scripts]
xob-bridge = "xiaozhi_openclaw_bridge.server:main"
